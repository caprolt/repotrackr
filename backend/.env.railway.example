# Railway Deployment Environment Variables
# Copy these variables to your Railway project settings

# =============================================================================
# DATABASE CONFIGURATION (Railway PostgreSQL)
# =============================================================================

# Railway will automatically provide DATABASE_URL, but you can override it
# DATABASE_URL=postgresql+asyncpg://postgres:[password]@[host]:5432/postgres
# DATABASE_URL_SYNC=postgresql://postgres:[password]@[host]:5432/postgres

# =============================================================================
# REDIS CONFIGURATION (Railway Redis)
# =============================================================================

# Railway will automatically provide REDIS_URL if you add a Redis service
# REDIS_URL=redis://[host]:[port]/[database]

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application settings
APP_NAME=RepoTrackr
DEBUG=false
ENVIRONMENT=production

# API settings
API_V1_STR=/api/v1
PROJECT_NAME=RepoTrackr
VERSION=0.1.0

# CORS settings (add your Vercel frontend domain)
# The frontend (Vercel) makes requests to this backend (Railway)
BACKEND_CORS_ORIGINS=["https://your-app.vercel.app", "http://localhost:3000"]

# =============================================================================
# SUPABASE CONFIGURATION (optional, for auth features)
# =============================================================================

# If using Supabase for additional features
# SUPABASE_URL=https://[project-ref].supabase.co
# SUPABASE_KEY=[your-service-role-key]
# SUPABASE_ANON_KEY=[your-anon-key]

# =============================================================================
# GITHUB INTEGRATION (for future use)
# =============================================================================

# GitHub API token for repository access
# GITHUB_TOKEN=

# GitHub webhook secret for repository events
# GITHUB_WEBHOOK_SECRET=

# =============================================================================
# RAILWAY DEPLOYMENT NOTES
# =============================================================================

# 1. Add a PostgreSQL service in Railway dashboard
# 2. Add a Redis service in Railway dashboard (optional, for job queue)
# 3. Railway will automatically inject DATABASE_URL and REDIS_URL
# 4. Set other environment variables in Railway project settings
# 5. Update BACKEND_CORS_ORIGINS with your Vercel domain
